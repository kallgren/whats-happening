{
  "name": "whats-happening",
  "private": true,
  "type": "module",
  "devDependencies": {
    "@vercel/node": "^5.1.0",
    "typescript": "^5.7.0"
  },
  "scripts": {
    "start": "vercel dev --listen 3000",
    "check": "tsc --noEmit"
  }
}
